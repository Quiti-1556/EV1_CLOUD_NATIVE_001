{
  "name": "solicitudes-session",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "build": "esbuild index.mjs --bundle --platform=node --target=node22 --format=esm --banner:js=\"import {createRequire} from 'node:module'; const require=createRequire(import.meta.url);\" --outfile=../.runtime/session-ms/index.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1000.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
